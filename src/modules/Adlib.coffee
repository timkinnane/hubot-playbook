# Accept and record template content by key (merge tag), save / load from brain
# remember( key, content )
# forget( key )
# ADLIB_REPLACE     String  Replace entire message if contains unknown key
# ADLIB_FALLBACK    String  Replace any unknown key with fallback content
# ADLIB_FALLBACK_*  String  Wildcard env var for matching keys (TODO as patch)
